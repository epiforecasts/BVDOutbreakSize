import{_ as t,o,c as s,ao as a}from"./chunks/framework.CDuh1GR-.js";const u=JSON.parse('{"title":"Contributing","description":"","frontmatter":{},"headers":[],"relativePath":"contributing.md","filePath":"contributing.md","lastUpdated":null}'),i={name:"contributing.md"};function n(r,e,d,c,l,h){return o(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="Contributing" tabindex="-1">Contributing <a class="header-anchor" href="#Contributing" aria-label="Permalink to &quot;Contributing {#Contributing}&quot;">​</a></h1><p>Issues and pull requests are welcome at <a href="https://github.com/epiforecasts/BVDOutbreakSize" target="_blank" rel="noreferrer">epiforecasts/BVDOutbreakSize</a>. The <a href="./">home page</a> covers installing the package, re-fitting the model, rendering the report and updating the data. This page covers the conventions for changing the project.</p><h2 id="Tasks" tabindex="-1">Tasks <a class="header-anchor" href="#Tasks" aria-label="Permalink to &quot;Tasks {#Tasks}&quot;">​</a></h2><p><code>Taskfile.yml</code> wraps the common commands, and <code>task --list</code> describes each one. The ones used while making a change:</p><ul><li><p><code>task format</code> runs Runic over <code>src/</code>, <code>test/</code>, <code>docs/</code>, <code>scripts/</code>, <code>benchmark/</code> and <code>ext/</code>.</p></li><li><p><code>task test</code> runs the full test suite. <code>task test-quick</code> skips the quality and AD items.</p></li><li><p><code>task fetch-fits</code> downloads the fits from the latest successful docs build, and <code>task docs</code> renders the site from them.</p></li><li><p><code>BVD_FIT_ID=&lt;id&gt; task fit</code> fits and caches one model, and <code>task fit-all</code> fits them all. <code>julia --project=docs docs/fits/list.jl</code> lists the ids.</p></li><li><p><code>task check-convergence</code> runs the convergence gate CI applies before publishing.</p></li><li><p><code>task release-notes</code> prints the notes the next release would publish.</p></li></ul><p>To render one report page from the cached fits, set <code>BVD_DOC_PAGE</code> to its path under <code>docs/pages/</code>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">BVD_DOC_PAGE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">estimates/national</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=docs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs/execute.jl</span></span></code></pre></div><p>The scripts under <code>scripts/</code> differ in which Julia project they need. Read <a href="https://github.com/epiforecasts/BVDOutbreakSize/blob/main/scripts/README.md" target="_blank" rel="noreferrer"><code>scripts/README.md</code></a> before running one.</p><h2 id="Model-code" tabindex="-1">Model code <a class="header-anchor" href="#Model-code" aria-label="Permalink to &quot;Model code {#Model-code}&quot;">​</a></h2><p>The model is built from Turing submodels in three layers under <code>src/models/</code>. <code>priors.jl</code> holds the building blocks, one per parameter family, each owning its own priors. <code>observations.jl</code> holds one observation submodel per data stream, each taking the growth state and adding its likelihood. <code>joint.jl</code> holds the composers that stitch these into full models, from the single-stream fits up to <code>bvd_joint</code>. The <a href="./methods">Methods</a> page describes the model and the <a href="./lib/api">API reference</a> lists every submodel.</p><ul><li><p>Submodels compose via <code>~ to_submodel(...)</code>.</p></li><li><p>A composer includes only the likelihoods for the streams it carries. A single-stream composer never instantiates the other observation submodels, because a discrete stream left sampled trips Turing&#39;s model check.</p></li><li><p>Pass a stream as <code>missing</code> to drop its likelihood. <code>bvd_joint</code> with every stream missing is the generator for the prior and posterior predictive checks.</p></li><li><p>Pass an optional component in as a submodel rather than switching it on with a flag, as in <code>background_pooling = background_pooling_model</code>. A flag reaches the model as a value rather than a type, so both arms are inferred on every build.</p></li><li><p>Keep the NaN and Inf safe clamps (<code>safe_nbinomial</code>, <code>eps</code>-flooring of expected counts) when editing a likelihood. They guard against extreme NUTS warmup proposals.</p></li><li><p>The AD backend is Mooncake reverse mode. The hand-written rules in <code>src/mooncake_rules.jl</code> are timed against the backend&#39;s own by <code>task benchmark-rules</code>. Run it after an AD backend upgrade and delete any rule that no longer pays for itself.</p></li><li><p>Code, code comments and docstrings keep to 80 characters per line.</p></li><li><p>Runic formats all Julia code. Its version is pinned in both <code>test/formatter/Project.toml</code> and <code>.pre-commit-config.yaml</code>, and <code>test/package/CodeFormatting.jl</code> fails if the two drift.</p></li><li><p>The DocStringExtensions templates in <code>src/docstrings.jl</code> add the signature to every docstring, so a docstring carries only its prose.</p></li><li><p>Comments describe the code as it is. The history of a change belongs in <a href="./news">News</a>.</p></li></ul><h3 id="Closures-in-model-code" tabindex="-1">Closures in model code <a class="header-anchor" href="#Closures-in-model-code" aria-label="Permalink to &quot;Closures in model code {#Closures-in-model-code}&quot;">​</a></h3><p>A closure in a model body must not box its captures.</p><p>Julia boxes a local variable when a closure captures it and something later reassigns it. The variable becomes a <code>Core.Box</code> and is type-unstable at every use. Mooncake answers type instability with a <code>DynamicDerivedRule</code>, a dictionary lookup per call site on every gradient evaluation, and Enzyme&#39;s reverse mode cannot differentiate through a box at all. Removing the boxes from the observation models roughly halved the gradient with the log-density bit-identical.</p><p>A box has no failure mode, which is why one can sit in the code for a long time. Gradients, the log-density and the parameter count are all correct, so the only symptom is an unexplained ratio of gradient time to primal time. A profiler does not localise it either, because the cost is charged to the frame that captures the variable rather than to the loop that pays it.</p><p>Three idioms produce a box.</p><ul><li><p>A widening guard that writes back in place, <code>if eltype(x) === Any; x = convert(...); end</code>, where a comprehension or <code>map</code> nearby reads <code>x</code>.</p></li><li><p>An accumulator rebound inside a loop whose comprehensions capture it.</p></li><li><p>A variable written on both arms of an <code>if</code>/<code>else</code> and then captured.</p></li></ul><p>The mere possibility of reassignment creates the box, whether or not the branch ever runs. A guard that almost never fires is therefore not free on the AD path. The branch itself costs nothing. The box it creates costs on every gradient.</p><p>The fix in every case is to compute into a separate binding and assign the captured name exactly once. Write a branch as an expression whose value is assigned, <code>x = if cond ... else ... end</code>, or as a ternary, rather than assigning <code>x</code> inside it.</p><p><code>test/test_boxed_captures.jl</code> enforces this for every method defined under <code>src/models/</code>. It walks the module&#39;s bindings rather than calling <code>methods</code> on the model constructors, because <code>@model</code> puts the model body in a gensym-named evaluator method and that generated method, not the user-facing constructor, is what AD differentiates. To check one method by hand:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">using BVDOutbreakSize</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">m = first(methods(BVDOutbreakSize.gate_before))</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">println(any(x -&gt; occursin(&quot;Core.Box&quot;, string(x)),</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    Base.uncompressed_ast(m).code))&#39;</span></span></code></pre></div><p><code>@code_warntype</code> reports the same thing, as a <code>Core.Box</code> in the variable list.</p><h2 id="Tests" tabindex="-1">Tests <a class="header-anchor" href="#Tests" aria-label="Permalink to &quot;Tests {#Tests}&quot;">​</a></h2><p>The tests use TestItemRunner. Each <code>test/test_*.jl</code> file holds the <code>@testitem</code>s for one feature. <code>test/runtests.jl</code> collects every item with <code>@run_package_tests</code>, so a new file needs no registering.</p><p>Tags split the suite across CI jobs.</p><ul><li><p><code>:quality</code> marks the Aqua, JET, formatting and doctest items.</p></li><li><p><code>:ad</code> marks the AD gradient checks.</p></li><li><p><code>:slow</code> marks the items that run full NUTS fits.</p></li></ul><p><code>runtests.jl</code> reads test arguments to choose among them. <code>skip_quality</code> drops the quality and AD items, <code>quality_only</code> and <code>ad_only</code> run one tag, and <code>fast</code> and <code>downgrade</code> drop all three.</p><p>To run one file, point <code>TestItemRunner.run_tests</code> at the <code>test/</code> directory with a filter and run it with <code>--project=test</code>:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> TestItemRunner</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">root </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> joinpath</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">pwd</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(), </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;test&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">TestItemRunner</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">run_tests</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    root; filter </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ti </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ti</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">filename </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> joinpath</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(root, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;test_renewal.jl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Scope the filter to this <code>test/</code> directory, or copies of the test files in sibling worktrees are collected too. Read the <code>Test Summary</code> line rather than the exit code.</p><h2 id="Report-pages" tabindex="-1">Report pages <a class="header-anchor" href="#Report-pages" aria-label="Permalink to &quot;Report pages {#Report-pages}&quot;">​</a></h2><p>Each rendered page is a Literate file under <code>docs/pages/</code>, in a folder per navigation group. Every page includes <code>docs/pages/_setup.jl</code>, which loads the observations and every fit through the cache. Anything two pages need lives there rather than on whichever page defined it first.</p><p><code>docs/execute.jl</code> renders one page. <code>docs/make.jl</code> renders the pages and assembles the Vitepress site, one stage at a time under <code>BVD_DOCS_STAGE</code>. CI renders each page in its own job and then runs the combine stage.</p><p>A new page needs:</p><ul><li><p>its Literate file under <code>docs/pages/</code></p></li><li><p>an entry in <code>PAGES</code>, a render stage and a navigation entry in <code>docs/make.jl</code></p></li><li><p>an entry in the page list in <code>docs/execute.jl</code></p></li><li><p>an entry in the render matrix in <code>.github/workflows/docs.yml</code></p></li><li><p>its rendered markdown in <code>.gitignore</code></p></li></ul><p>Setup and table-construction code sits inside <code>&lt;details&gt;</code> dropdowns via <code>#md # @raw html</code> blocks. The bare result object follows with <code>#hide</code>, so only the output renders.</p><p>The shared front matter (title, authors, abstract, scope) is single-sourced in <code>README.md</code>, up to the <code>&lt;!-- SHARED:END --&gt;</code> marker. Edit it in <code>README.md</code> only. <code>docs/front_matter.jl</code> reads it at build time and fills in the dates. <code>docs/make.jl</code> copies the whole README to the home page, so do not duplicate it into a report page.</p><h2 id="Fits-and-the-fit-cache" tabindex="-1">Fits and the fit cache <a class="header-anchor" href="#Fits-and-the-fit-cache" aria-label="Permalink to &quot;Fits and the fit cache {#Fits-and-the-fit-cache}&quot;">​</a></h2><p>Fits are cached under <code>logs/fit_cache</code>, keyed on a content hash. <code>fit_content_hash</code> in <code>docs/fits/registry.jl</code> builds the hash from three inputs.</p><ul><li><p>The bytes of each file in <code>FIT_SOURCE_FILES</code>: the files under <code>src/models/</code>, <code>renewal.jl</code>, <code>sampling.jl</code>, <code>constants.jl</code>, <code>data.jl</code> and <code>onset_curve.jl</code> in <code>src/</code>, and the cache code itself.</p></li><li><p>Every file under <code>data/</code> except those named in <code>FIT_DATA_EXCLUDE</code>.</p></li><li><p>The cache schema version and the sampler settings, including <code>joint_sampler_args()</code> for the headline joint and its spatial control.</p></li></ul><p>Any edit to one of those files, a comment included, changes the key for every fit. In CI that is a cold refit of every model, which takes hours. Plotting, summary and reporting code sits outside the key, so a report change reuses the cached fits.</p><p>Every file <code>scripts/score_releases.jl</code> writes into <code>data/</code> must be listed in <code>FIT_DATA_EXCLUDE</code>. Otherwise the render&#39;s hash differs from the fit matrix&#39;s and every fit misses.</p><p>The render never fits. A fit missing from the cache fails the build naming its key. <code>BVD_FIT_STRICT=false</code> restores inline fitting, for a page run outside the cache entirely.</p><p>A cached fit does not survive a version change in Turing or its dependencies. Refit rather than debugging a <code>KeyError</code> on a stale chain. Any change to the model, the priors or the data needs a refit before its results mean anything.</p><p>Every NUTS fit writes a progress log to <code>logs/&lt;fit&gt;.log</code> and a TensorBoard run under <code>logs/tensorboard/&lt;fit&gt;/</code>. <code>BVD_FIT_LOG</code> controls this (<code>all</code> when unset, or <code>progress</code>, <code>tensorboard</code>, <code>none</code>). <code>task tensorboard</code> shows every fit in the worktree.</p><h2 id="Prose" tabindex="-1">Prose <a class="header-anchor" href="#Prose" aria-label="Permalink to &quot;Prose {#Prose}&quot;">​</a></h2><ul><li><p>One sentence per line in markdown and write-up prose. Do not wrap prose at 80 characters.</p></li><li><p>UK English throughout.</p></li><li><p>Write in the present tense and describe the current state. Development history belongs in <a href="./news">News</a> and nowhere else.</p></li></ul><h3 id="Analysis-report-prose" tabindex="-1">Analysis report prose <a class="header-anchor" href="#Analysis-report-prose" aria-label="Permalink to &quot;Analysis report prose {#Analysis-report-prose}&quot;">​</a></h3><p>These apply to the narrative prose in the report pages under <code>docs/pages/</code>, and to write-up prose generally. Use the existing report text as the template for tone. The measured sentence- and paragraph-level rules below were reverse-engineered from a manuscript the maintainers are happy with. The repo-specific rules that follow take precedence where the two disagree. <code>AGENTS.md</code> carries a short version of this section for agent sessions, pointing back here for the full rule. Keep the two in agreement: if this section changes in a way that affects the summary, update <code>AGENTS.md</code> too.</p><ul><li><p>No code references in the narrative. Do not name functions, parameters, files, or <code>:symbols</code> in the prose. Describe each quantity in words, and define a derived quantity in words the first time it appears, near its figure or table.</p></li><li><p>Concise and direct. Cut filler and adjectives. Avoid the LLM-indicator words: comprehensive, leverage, robust, framework (when vague), utilise, facilitate, novel, landscape, foster, harness, streamline, pivotal, nuanced, multifaceted, cornerstone, synergy, overarching. Avoid filler and stance markers: very, significant, crucial, essential, clearly, obviously, &quot;it is important to note that&quot;. Delete the LLM connective openers <code>Furthermore</code>, <code>Moreover</code>, <code>Additionally</code>, <code>In conclusion</code>, <code>Overall</code>. Restrict genuine connectives to <code>However,</code> <code>Whilst</code>, <code>Yet</code>, <code>Unlike</code>, <code>Similarly</code>, <code>Alternatively</code>, <code>Finally,</code>, <code>Instead</code>.</p></li><li><p>Target 15-30 words per sentence and treat 40 as the ceiling. Exceed it only for a flat, colon-led enumeration, never for nested subordination. Use at most two commas per sentence for internal structure, a third only in a serial list.</p></li><li><p>Never use a dash as punctuation. Replace it with a full stop, or with parentheses when the aside is a gloss or example. Use a colon only to introduce a list or an expansion, never to join two independent statements. Use a semicolon only to separate list items that themselves contain commas.</p></li><li><p>Instead of a trailing qualifier (&quot;, which means that...&quot;, &quot;, thereby enabling...&quot;), cut the comma and start a new sentence with <code>This</code>, <code>These</code> or <code>It also</code>. Keep any surviving trailing <code>, which</code> clause to about one sentence in ten, and only where the clause carries the point rather than padding it.</p></li><li><p>Write paragraphs of 2-6 sentences. Open each with its claim, not with context, and close it with a consequence or a limitation, never a restatement of the opening.</p></li><li><p>Hedge with <code>can</code>, <code>could</code>, <code>may</code> or <code>potential</code>, one hedge per claim. Never stack hedges (&quot;may potentially&quot;, &quot;could possibly suggest&quot;).</p></li><li><p>Report intervals as sentences, without a leading median. Write the credible interval as a phrase, not a &quot;median (lower, upper)&quot; construction.</p></li><li><p>Report numbers with their provenance and units. Delete a vague quantity (&quot;a large number of&quot;, &quot;significantly more&quot;) rather than leave it unsourced.</p></li><li><p>UK English throughout.</p></li><li><p>Section and subsection titles are just the title. No descriptive suffix after a title (not &quot;Reproduction number — weekly random walk with intervention ramp&quot;, just &quot;Reproduction number&quot;), and no detail-dump in the first sentence after a heading.</p></li><li><p>Order the methods generatively, infections through to observation endpoints: the infection process first, then the epidemiological processes (delays, case-fatality ratio), then the observation models (surveillance streams before exports), then the joint model.</p></li><li><p>Define every quantity before it is used. Define the reproduction number before the seeding that relies on it; introduce the initial infection count before describing how it arises; define every symbol and operator (including convolution) the first time it appears. Never use a symbol the reader has not met. Give an equation once in display maths and refer to its symbols inline afterwards; never narrate a displayed equation in words.</p></li><li><p>Do not repeat. State a convention once (the credible-interval levels, the delay discretisation) and do not restate it per bullet or subsection. Cut sentences that duplicate earlier content.</p></li><li><p>Cite the source of each prior and carry the uncertainty the source reports. When a source gives a distribution with uncertainty (a shape and scale with intervals), propagate that, not a self-assigned weakly-informative spread. Do not write &quot;with an assumed weakly-informative spread&quot; repeatedly. If a prior is our own choice, say so plainly (&quot;we use a prior of ...&quot;).</p></li><li><p>State assumptions as assumptions (&quot;we assume a single seed case&quot;, &quot;we assume the response scale-up takes about three weeks&quot;). Do not assert a false rationale for a modelling choice (not &quot;a Poisson because the count is small&quot;).</p></li><li><p>Do not editorialise or justify priors in the narrative (not &quot;a diffuse prior would let the background absorb the whole stream&quot;). State what the model does.</p></li><li><p>Methods belong in the methods. Do not leave model description (the intervention model, the counterfactual, the forecast, the evaluation) in the results. Move it to the methods and keep the results to findings.</p></li><li><p>No project or issue history in the narrative: no PR or issue numbers, no &quot;we previously used X&quot;, &quot;this was changed to Y&quot;, &quot;after refactoring&quot;. State the current design only. A difference from work being replicated, or a limitation of an external dependency, is the exception, written as a present-tense fact. An issue number may also stay where the issue is the record for something still unresolved, such as a workaround pointing at the open root cause it does not fix, or a data decision whose reasoning lives in the issue. The test is whether the number still tells the reader something they cannot get from the code. &quot;Tracked by #495&quot; earns its place; &quot;changed in #495&quot; does not.</p></li><li><p>Label quantities accurately. Do not call suspected cases onsets, and prefer &quot;current cumulative&quot; over &quot;final cumulative&quot;.</p></li><li><p>For a latent quantity (infections, onsets, deaths) report the modelled estimate without overlaying observed data that sits downstream of unmodelled processes.</p></li><li><p>Plots use the same credible-interval ribbons as the tables, not a bare median, and show only the period being estimated rather than greying out the rest.</p></li><li><p>Model code shown in the report is clean. Strip working comments before it is displayed.</p></li><li><p>Flag a future improvement as a GitHub issue, not a buried caveat in the prose.</p></li><li><p>Bullet lists, bold and italics are advisory rather than enforced here, since this is a technical report rather than a journal manuscript. Do not add new bullet lists or bold to the narrative prose, and do not restructure an existing list that carries genuinely parallel content, but do strip bold or italics used for mid-sentence emphasis.</p></li></ul><h2 id="Commits" tabindex="-1">Commits <a class="header-anchor" href="#Commits" aria-label="Permalink to &quot;Commits {#Commits}&quot;">​</a></h2><p>Commit messages follow Conventional Commits, <code>type(scope): summary</code>, with a lower-case imperative summary. The types in use are <code>feat</code>, <code>fix</code>, <code>docs</code>, <code>test</code>, <code>refactor</code>, <code>perf</code>, <code>ci</code>, <code>chore</code> and <code>style</code>, plus <code>data</code> for a change to the observations. The scope names the area touched, as in <code>fix(scripts)</code>, <code>docs(news)</code> or <code>perf(ad)</code>.</p><h2 id="News" tabindex="-1">News <a class="header-anchor" href="#News" aria-label="Permalink to &quot;News {#News}&quot;">​</a></h2><p>Every user-visible change adds an entry to <a href="./news">News</a> as part of the change itself. The file holds one section per version, newest first. The top section is the open one, and its heading matches the version in <code>Project.toml</code>. Add to that section rather than starting a new one.</p><p>Within a version, entries sit under <code>### Model</code>, <code>### Data</code>, <code>### Report</code>, <code>### Performance</code>, <code>### Fixed</code>, <code>### Infrastructure</code> or <code>### Dependencies</code>. An entry says what changed and why, in the present tense, with the pull request or issue number in brackets. Say whether fitted values change. Write one sentence per line.</p><h2 id="Pull-requests-and-CI" tabindex="-1">Pull requests and CI <a class="header-anchor" href="#Pull-requests-and-CI" aria-label="Permalink to &quot;Pull requests and CI {#Pull-requests-and-CI}&quot;">​</a></h2><p><code>main</code> is branch-protected, so changes go through pull requests to <code>epiforecasts/BVDOutbreakSize</code>. Everything here is slow. The full test suite takes a long time and a full docs build fits every model. Open the pull request early and let CI do the long work. Run <code>task format</code> before every push, then only narrow checks locally: <code>task test-quick</code>, a single-file test run, or one rendered page.</p><p>CI runs the test suite (<code>.github/workflows/test.yml</code>) and builds the docs, publishing <code>output/</code> as a GitHub Release on each push to <code>main</code> (<code>.github/workflows/docs.yml</code>).</p><p>On a pull request each of those runs only when the change touches something it is built from. The test suite and coverage need <code>src/</code>, <code>ext/</code>, <code>test/</code>, <code>data/</code>, <code>Project.toml</code>, and <code>docs/fits/</code> and <code>scripts/</code> because test items include files from both. The report needs <code>src/</code>, <code>ext/</code>, <code>data/</code>, <code>docs/</code>, <code>scripts/</code>, <code>README.md</code> and <code>Project.toml</code>. A workflow that skips says so in the summary of its <code>changes</code> job, so a skipped build is visible rather than being an absent check. A push to <code>main</code>, a tag and a manual run are never gated.</p><p>The lists live in each workflow&#39;s <code>changes</code> job and are checked by <code>.github/actions/changed-paths/patterns_test.sh</code>, which pre-commit runs whenever one of them is edited. Widen the list when something new feeds a build: a pattern that is too narrow skips the job that would have caught the change, and nothing reports that as a failure.</p><h3 id="Before-asking-for-review" tabindex="-1">Before asking for review <a class="header-anchor" href="#Before-asking-for-review" aria-label="Permalink to &quot;Before asking for review {#Before-asking-for-review}&quot;">​</a></h3><p>These are the issues most often flagged in review. Check the whole diff against <code>main</code> for them before asking for one.</p><ul><li><p>Comments, docstrings and test names state what the code does now, briefly. History belongs in <a href="./news">News</a>.</p></li><li><p>Every claim in a comment, docstring, news entry or pull request description is true of the code in the diff. That includes cross-references, and any unchanged text next to the change that the change makes stale.</p></li><li><p>A number in the news or a pull request description names its source: a benchmark comment, a job log or a committed script.</p></li><li><p>Nothing is duplicated. Reuse an existing helper, and write a test&#39;s reference implementation from the formula rather than copying the code under test.</p></li><li><p>Nothing is added or exported without a consumer outside its own tests, and code the change leaves unused is removed.</p></li><li><p>A new function matches its siblings in keywords, index ranges, missing-value checks and error messages.</p></li><li><p>Report pages follow &quot;Analysis report prose&quot; above.</p></li><li><p>New behaviour has a test that fails if the change is reverted.</p></li></ul><h2 id="Releases" tabindex="-1">Releases <a class="header-anchor" href="#Releases" aria-label="Permalink to &quot;Releases {#Releases}&quot;">​</a></h2><p>A release is cut by commenting <code>@release</code> on any issue or pull request. <code>.github/workflows/release.yml</code> tags <code>main</code>, publishes a GitHub release whose notes are the newest section of <code>docs/src/news.md</code>, and opens a pull request bumping the version and starting the next section. <code>@release minor</code> and <code>@release major</code> choose the size of that bump; plain <code>@release</code> is a patch. <code>task release-notes</code> prints what would be published, so the notes can be read before anything is cut.</p>`,65)])])}const m=t(i,[["render",n]]);export{u as __pageData,m as default};
