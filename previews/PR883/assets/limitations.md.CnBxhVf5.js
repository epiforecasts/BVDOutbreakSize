import{_ as a,o as t,c as i,ao as o}from"./chunks/framework.B3JnvKDd.js";const u=JSON.parse('{"title":"Limitations","description":"","frontmatter":{},"headers":[],"relativePath":"limitations.md","filePath":"limitations.md","lastUpdated":null}'),r={name:"limitations.md"};function n(s,e,l,d,h,c){return t(),i("div",null,[...e[0]||(e[0]=[o('<h1 id="Limitations" tabindex="-1">Limitations <a class="header-anchor" href="#Limitations" aria-label="Permalink to &quot;Limitations {#Limitations}&quot;">​</a></h1><h2 id="Overall" tabindex="-1">Overall <a class="header-anchor" href="#Overall" aria-label="Permalink to &quot;Overall {#Overall}&quot;">​</a></h2><ul><li><p>The model is fitted to aggregate counts, with the DRC data national and per-province situation-report totals read from the published PDFs, and the Uganda data three export cases with one death from WHO reports. We do not have a line list, information on case definitions or reporting completeness, or access to the response&#39;s internal data, so every estimate is a model-based extrapolation under strong assumptions, not a measurement.</p></li><li><p>The work is done outside the outbreak response, so changes in reporting practice are inferred from the reports rather than known.</p></li><li><p>The code and analysis were drafted by a language model and reviewed by people and by language models (<a href="/BVDOutbreakSize/previews/PR883/about#Authors,-funding-and-acknowledgements">authors</a>).</p></li><li><p>The report is re-run as new data arrive, so the estimates change between updates, and it has not been peer reviewed. Every release is signed off by a person before it is published.</p></li></ul><h2 id="National" tabindex="-1">National <a class="header-anchor" href="#National" aria-label="Permalink to &quot;National {#National}&quot;">​</a></h2><h3 id="Data" tabindex="-1">Data <a class="header-anchor" href="#Data" aria-label="Permalink to &quot;Data {#Data}&quot;">​</a></h3><ul><li><p>Most quantities, including nearly all of the delays, the case-fatality ratio and the laboratory assumptions, rest on weakly-informed priors, often from other outbreaks, and their posteriors largely track those priors. The onset-to-report delay is the exception, informed by the <a href="/BVDOutbreakSize/previews/PR883/methods#Symptom-onset-reporting-delay">onset curve</a>.</p></li><li><p>Almost every count is report-dated, so the epidemic&#39;s timing is recovered mainly through the assumed delays. The digitised onset curve, covering confirmed cases from SitRep 059 onward, is the only series carrying symptom-onset dates.</p></li><li><p>The suspected streams are no longer published, so the late window rests on the confirmed, laboratory, treatment-centre and onset data, and the suspected forecasts cannot be checked. The laboratory analysed-specimen series covers only part of the window (<a href="/BVDOutbreakSize/previews/PR883/methods#Laboratory-pipeline">laboratory pipeline</a>).</p></li><li><p>Later situation reports can revise earlier totals up or down as suspects are reclassified and newly-reporting health zones are added, and we do not model this revision process.</p></li><li><p>The onset curve is digitised from a figure that each report redraws, so the counts carry scan error that the model treats as noise at a fixed scale.</p></li></ul><h3 id="Model" tabindex="-1">Model <a class="header-anchor" href="#Model" aria-label="Permalink to &quot;Model {#Model}&quot;">​</a></h3><ul><li><p>The streams observe overlapping people but are fitted as conditionally independent given latent incidence, which can understate uncertainty. The <a href="/BVDOutbreakSize/previews/PR883/sensitivity#Outbreak-size-estimated-by-each-data-stream">outbreak size estimated by each data stream</a> checks whether they agree.</p></li><li><p>The DRC ascertainment and the testing fraction are each one value over the window, although ascertainment probably rose, so a change in either is read as a change in incidence.</p></li><li><p>The model inherits McCabe et al.&#39;s assumptions of a single zoonotic seed, a generation interval from earlier Ebola outbreaks and no depletion of susceptibles, which help constrain the estimated outbreak age and early reproduction number. The onset-to-death delay and the <a href="/BVDOutbreakSize/previews/PR883/methods#Genetic-bound-on-outbreak-age">genetic seeding bound</a> do not propagate cross-outbreak or clock uncertainty.</p></li><li><p>The intervention ramp in the <a href="/BVDOutbreakSize/previews/PR883/methods#Reproduction-number">reproduction number</a> is fixed in time, with an assumed start and three-week duration, and only its size is estimated.</p></li><li><p>Occupancy shows demand only up to the beds filled, so the bed shortfall above capacity is not measured and comes from the <a href="/BVDOutbreakSize/previews/PR883/methods#Treatment-centre-flow">treatment-centre flow</a> model and its priors.</p></li></ul><h3 id="Evaluation" tabindex="-1">Evaluation <a class="header-anchor" href="#Evaluation" aria-label="Permalink to &quot;Evaluation {#Evaluation}&quot;">​</a></h3><ul><li><p>The interval on the <a href="/BVDOutbreakSize/previews/PR883/methods#Symptom-onset-nowcast-and-forecast">onset forecast</a> is dominated by per-scan error rather than the epidemic.</p></li><li><p>The persistence baseline for a frozen re-fit reads a data snapshot that can post-date the forecast by weeks, so it can see later corrections (<a href="/BVDOutbreakSize/previews/PR883/methods#Forecast-scoring-against-a-persistence-baseline">details</a>).</p></li><li><p>Past forecasts that were not stored are rebuilt from each release&#39;s own code but with current dependency versions, so they are not exact.</p></li></ul><h2 id="Provinces" tabindex="-1">Provinces <a class="header-anchor" href="#Provinces" aria-label="Permalink to &quot;Provinces {#Provinces}&quot;">​</a></h2><h3 id="Data-2" tabindex="-1">Data <a class="header-anchor" href="#Data-2" aria-label="Permalink to &quot;Data {#Data-2}&quot;">​</a></h3><ul><li><p>Confirmed cases and deaths are the only province data fitted. Occupancy and beds by province are not, and there is no province onset nowcast.</p></li><li><p>The province tables start on 15 June, and harmonisation backfill is published only nationally. Before then the provinces are informed only through the national streams (<a href="/BVDOutbreakSize/previews/PR883/evaluation/insample/province#province-compositions">province compositions</a>).</p></li></ul><h3 id="Model-2" tabindex="-1">Model <a class="header-anchor" href="#Model-2" aria-label="Permalink to &quot;Model {#Model-2}&quot;">​</a></h3><ul><li><p>Ituri, Nord-Kivu and Haut-Uele are modelled individually and every other affected province is pooled into a fourth patch, each well mixed, so spread inside a province is not represented.</p></li><li><p>There is no mobility or origin-destination data for this outbreak, so the <a href="/BVDOutbreakSize/previews/PR883/methods#Mixing-and-importation">gravity kernel</a> coupling the provinces is assumed, not measured.</p></li><li><p>Provincial testing enters the prior, not the likelihood, so the deaths do most of the work in separating a province&#39;s incidence from its case-finding and the prior strongly influences the rest (<a href="/BVDOutbreakSize/previews/PR883/estimates/province#Province-parameters-against-their-priors">province parameters</a>).</p></li><li><p>The treatment-centre model carries one national bed capacity and demand, so it cannot represent local saturation. Ituri holds most of the occupied beds, so the national bed shortfall understates local unmet need.</p></li></ul>',15)])])}const m=a(r,[["render",n]]);export{u as __pageData,m as default};
