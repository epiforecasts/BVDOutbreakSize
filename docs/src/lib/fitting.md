# Fitting

Running NUTS over a composed model, initialising from a viable prior draw, and the callbacks that stream progress to a log and to TensorBoard.
`fit_parallel` runs independent fits side by side, bounded by the available threads.

## Index

```@index
Pages = ["fitting.md"]
```

## Reference

```@autodocs
Modules = [BVDOutbreakSize]
Pages = ["sampling.jl"]
Private = false
```
