# Joint and single-stream models

The composers that assemble the submodels into a fittable model.
`bvd_joint` is the headline fit over every included stream; the `*_only_model` composers fit one stream at a time for the stream-by-stream comparison.

## Index

```@index
Pages = ["joint.md"]
```

## Reference

```@autodocs
Modules = [BVDOutbreakSize]
Pages = ["models/joint.jl"]
Private = false
```
