# Plotting

Every figure in the report.
The functions take a chain and the observations and return a Makie `Figure`, so a page renders a figure in one call.

## Index

```@index
Pages = ["plotting.md"]
```

## Reference

```@autodocs
Modules = [BVDOutbreakSize]
Pages = ["plots.jl"]
Private = false
```
