import{_ as t,o,c as s,ao as i}from"./chunks/framework.DfJvOW90.js";const u=JSON.parse('{"title":"Contributing","description":"","frontmatter":{},"headers":[],"relativePath":"contributing.md","filePath":"contributing.md","lastUpdated":null}'),a={name:"contributing.md"};function n(r,e,d,l,c,h){return o(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="Contributing" tabindex="-1">Contributing <a class="header-anchor" href="#Contributing" aria-label="Permalink to &quot;Contributing {#Contributing}&quot;">​</a></h1><p>Issues and pull requests are welcome at <a href="https://github.com/epiforecasts/BVDOutbreakSize" target="_blank" rel="noreferrer">epiforecasts/BVDOutbreakSize</a>. This page covers how the project is laid out, how to run it, and the conventions to follow when changing it.</p><h2 id="Repository-layout" tabindex="-1">Repository layout <a class="header-anchor" href="#Repository-layout" aria-label="Permalink to &quot;Repository layout {#Repository-layout}&quot;">​</a></h2><ul><li><p><code>src/BVDOutbreakSize.jl</code> — the module entry point: dependencies, the export list, and the <code>include</code> order for the rest of <code>src/</code>. The functionality itself is split across single-purpose files, listed next.</p></li><li><p><code>src/data.jl</code> — <code>load_observations</code> and <code>freeze_observations</code>, the observation-manifest loader.</p></li><li><p><code>src/constants.jl</code> — fixed constants, including the published Imperial point estimates (<code>REPORT_SCENARIOS</code>, <code>REPORT_SCENARIOS_CI</code>).</p></li><li><p><code>src/sampling.jl</code> — NUTS sampling (<code>nuts_sample</code>, <code>fit_parallel</code>) and the AD backend setup.</p></li><li><p><code>src/renewal.jl</code> — the shared renewal-process helpers: <code>renewal_infections</code>, the delay convolutions <code>convolve_delay</code>, <code>convolve_survival</code> and <code>convolve_pmf</code>, and <code>discretise_censored</code>.</p></li><li><p><code>src/models/priors.jl</code>, <code>src/models/observations.jl</code>, <code>src/models/joint.jl</code> — the building-block submodels, the observation submodels and the composers. See <a href="/BVDOutbreakSize/v1.15/contributing#Model-architecture">Model architecture</a> below.</p></li><li><p><code>src/summaries.jl</code>, <code>src/scoring.jl</code> — summary and comparison tables, and forecast scoring.</p></li><li><p><code>src/counterfactual.jl</code> — the no-onward-deaths projection (<code>predict_no_onward_deaths</code>).</p></li><li><p><code>src/forecast.jl</code> — forecast helpers (<code>forecast_reported</code>).</p></li><li><p><code>src/confirmed_cfr.jl</code> — delay-corrected confirmed-case-fatality-ratio helpers.</p></li><li><p><code>src/plots.jl</code> — plotting.</p></li><li><p><code>docs/examples/analysis.jl</code> and <code>docs/examples/sensitivity.jl</code> — the Literate walkthroughs, split so the expensive fits and the render can fan out across CI runners. <code>analysis.jl</code> carries the methods, results and one-week-ahead forecast. <code>sensitivity.jl</code> carries the forecast validation and the comparison/sensitivity analyses. Both load their fits through the shared <code>docs/examples/_setup.jl</code>. <code>analysis.jl</code> is the main artifact.</p></li><li><p><code>docs/fits/</code> — the fit-cache machinery: <code>registry.jl</code> (the fit-id list), <code>cache.jl</code> (content-addressed fit caching under <code>logs/fit_cache</code>), <code>one.jl</code> (fit and cache a single id, <code>task fit</code>), <code>all.jl</code> (fit every model, <code>task fit-all</code>), and <code>list.jl</code> (print fit ids for the CI matrix).</p></li><li><p><code>docs/execute.jl</code> — runs one Literate page against the fit cache and writes its markdown, figures and half of <code>output/</code> (used by <code>task docs-main</code> and <code>task docs-sensitivity</code>).</p></li><li><p><code>docs/make.jl</code> — the Vitepress combine step: copies <code>README.md</code> to <code>index.md</code>, assembles the site from the already-rendered markdown, and builds the bibliography (used by <code>task docs</code>).</p></li><li><p><code>data/observations.toml</code> — single source of truth for observation data (case and death counts, traveller volumes, sources). Loaded via <code>load_observations()</code> and never hardcoded. Update this one file for a new situation report and the analysis picks it up. The literate re-binds its observation <code>const</code>s from the loaded TOML, so the package constants are defaults only.</p></li><li><p><code>scripts/run.jl</code> — regenerates published results by including the literate and writes CSVs to <code>output/</code>.</p></li><li><p><code>test/</code> — one file per feature, driven by <code>test/runtests.jl</code>, plus <code>enzyme/</code>, <code>formatter/</code>, <code>jet/</code> and <code>package/</code> for the quality checks.</p></li><li><p><code>external/bdbv-linelist-analysis</code> — git submodule, source of the onset-to-death delay priors.</p></li></ul><h2 id="Running-and-testing" tabindex="-1">Running and testing <a class="header-anchor" href="#Running-and-testing" aria-label="Permalink to &quot;Running and testing {#Running-and-testing}&quot;">​</a></h2><p><code>Taskfile.yml</code> at the repository root wraps the common commands. Run <code>task --list</code> for the full set with descriptions. The ones used day to day:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Instantiate the package environment (no task wraps this)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;using Pkg; Pkg.instantiate()&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Fit and cache every model, then render the docs from the cache</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">task</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> fit-all</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">task</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Fit and cache a single model by id (list ids with</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># \`julia --project=docs docs/fits/list.jl\`)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">BVD_FIT_ID</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">deaths</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> task</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> fit</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Render just the main analysis page from the cache, for fast iteration</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">task</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs-main</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">task</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs-sensitivity</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Format (SciML style) over src/, test/, docs/, scripts/</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">task</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> format</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Run the full test suite, or skip the quality checks (Aqua/JET/format)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># for a quicker one</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">task</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">task</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test-quick</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Regenerate the published output CSVs into output/ (no task wraps this)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=.</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> scripts/run.jl</span></span></code></pre></div><p>Running <code>julia --project=. docs/examples/analysis.jl</code> directly instead steps through the full narrative without going through the fit cache, fitting every model inline. This is the slow path. Prefer <code>task fit-all &amp;&amp; task docs</code> for anything beyond reading the source.</p><p>A build streams per-fit progress by default: every NUTS fit writes <code>logs/&lt;fit&gt;.log</code> (iteration, log-density, divergences) and a TensorBoard run under <code>logs/tensorboard/&lt;fit&gt;/</code>, controlled by <code>BVD_FIT_LOG</code> (<code>all</code> when unset, or <code>progress</code>, <code>tensorboard</code>, <code>none</code>). CI release builds set <code>BVD_FIT_LOG=none</code>. Tail a log for quick liveness, or run <code>task tensorboard</code> to view all fits in the worktree. The logs live under the git-ignored <code>logs/</code>, so each worktree keeps its own. A cached fit lives under <code>logs/fit_cache</code> and is keyed by a content hash of the fit-relevant code and data, so a Turing or dependency version bump invalidates it. Refit rather than debugging a <code>KeyError</code> on a stale chain.</p><p><code>test/runtests.jl</code> includes each <code>test/test_*.jl</code>. To iterate on one file, run it inside a REPL after <code>using BVDOutbreakSize</code>, or temporarily comment out the others in <code>runtests.jl</code>.</p><p>CI runs the test suite (<code>.github/workflows/test.yml</code>) and builds the docs, publishing <code>output/</code> as a GitHub Release on each push to <code>main</code> (<code>.github/workflows/docs.yml</code>).</p><h2 id="Model-architecture" tabindex="-1">Model architecture <a class="header-anchor" href="#Model-architecture" aria-label="Permalink to &quot;Model architecture {#Model-architecture}&quot;">​</a></h2><p>The model is assembled from small, swappable Turing submodels rather than one monolithic block (the build-up is drawn as a flowchart on the <a href="./analysis">Analysis</a> page). There are three layers.</p><p><strong>Building-block submodels</strong>, one per parameter family, each owning its own priors:</p><ul><li><p><code>exponential_growth_model</code> samples the doubling time <code>τ</code> and the doubling-time multiplier <code>m = T/τ</code>, not <code>τ</code> and <code>T</code> directly, to break the <code>C(T) = exp(rT)</code> ridge.</p></li><li><p><code>onset_to_death_model</code> is the gamma onset-to-death delay, built by convolving two atomic <code>gamma_delay_model</code> delays (onset→admission, admission→death) rather than fitting the onset→death delay directly.</p></li><li><p><code>cfr_model</code> is the case-fatality ratio.</p></li><li><p><code>surveillance_dispersion_model</code> samples on the <code>1/√k</code> scale.</p></li><li><p><code>pooled_ascertainment_model</code> partially pools the DRC and Uganda reporting fractions <code>p_drc</code> and <code>p_uganda</code> on the logit scale.</p></li></ul><p><strong>Observation submodels</strong>, one per data stream, each taking the growth state, adding its forward integral and likelihood: <code>exports_model</code> and <code>exports_deaths_model</code> (the Uganda imports and import deaths, each an inhomogeneous Poisson process over the dated detection days rather than a rectangular detection window), <code>deaths_model</code>, <code>reported_cases_model</code>, <code>confirmed_cases_model</code> and <code>confirmed_deaths_model</code> (NegBinomial DRC surveillance and laboratory streams), and <code>treatment_flow_model</code> and <code>recovered_model</code> (the isolation-occupancy and recovered-among-confirmed streams).</p><p><strong>Composers</strong> stitch the blocks into full generative models: <code>exports_only_model</code>, <code>deaths_only_model</code>, <code>cases_only_model</code>, <code>confirmed_only_model</code>, <code>confirmed_deaths_only_model</code>, <code>treatment_only_model</code>, <code>exports_deaths_only_model</code>, <code>exports_joint_only_model</code> (the Uganda export cases and deaths fitted jointly over one shared at-risk prevalence), and <code>bvd_joint</code> (every stream together). Each composer conditionally includes only the likelihoods for the streams it carries. A single-stream composer never instantiates the other observation submodels, so a discrete stream is never left sampled, which would trip Turing&#39;s model check. Pass a stream as <code>missing</code> to drop its likelihood. <code>bvd_joint</code> with all streams missing is the generator used for the prior and posterior predictive checks.</p><h2 id="Conventions" tabindex="-1">Conventions <a class="header-anchor" href="#Conventions" aria-label="Permalink to &quot;Conventions {#Conventions}&quot;">​</a></h2><ul><li><p>Maximum 80 characters per line of code.</p></li><li><p>One sentence per line in write-up prose and markdown. Do not wrap prose at 80 characters. Code, code comments and docstrings keep the 80-character limit and wrap normally. This rule is for prose only.</p></li><li><p>The shared front matter (title, authors, abstract, scope) is single-sourced in <code>README.md</code>, up to the <code>&lt;!-- SHARED:END --&gt;</code> marker. Edit it in <code>README.md</code> only. <code>docs/examples/analysis.jl</code> loads it at build time via a Documenter <code>@eval</code> block that reads <code>README.md</code> and extracts everything before that marker, so do not duplicate it into the analysis page.</p></li><li><p>Table-construction and other setup code in <code>analysis.jl</code> is hidden inside <code>&lt;details&gt;</code> dropdowns via <code>#md # @raw html</code> blocks. The bare result object follows (with <code>#hide</code>) so only the output renders.</p></li><li><p>The surveillance dispersion prior is a half-normal <code>truncated(Normal(0.6, 0.2); lower = 0)</code> on <code>inv_sqrt_k</code>, following the Stan prior-choice recommendations.</p></li><li><p>Docstrings use DocStringExtensions (<code>$(TYPEDSIGNATURES)</code>).</p></li><li><p>Two implementation details: the AD backend is Mooncake reverse-mode, and models compose via <code>~ to_submodel(...)</code>.</p></li><li><p>NaN and Inf safe clamps (<code>safe_nbinomial</code>, <code>eps</code>-flooring of expected counts) guard against extreme NUTS warmup proposals. Keep them when editing the likelihoods.</p></li></ul><h3 id="Analysis-report-prose" tabindex="-1">Analysis report prose <a class="header-anchor" href="#Analysis-report-prose" aria-label="Permalink to &quot;Analysis report prose {#Analysis-report-prose}&quot;">​</a></h3><p>These apply to the narrative prose in <code>docs/examples/analysis.jl</code>, and to write-up prose generally. Use the existing report text as the template for tone. The measured sentence- and paragraph-level rules below were reverse-engineered from a manuscript the maintainers are happy with. The repo-specific rules that follow take precedence where the two disagree. <code>AGENTS.md</code> carries a short version of this section for agent sessions, pointing back here for the full rule. Keep the two in agreement: if this section changes in a way that affects the summary, update <code>AGENTS.md</code> too.</p><ul><li><p>No code references in the narrative. Do not name functions, parameters, files, or <code>:symbols</code> in the prose. Describe each quantity in words, and define a derived quantity in words the first time it appears, near its figure or table.</p></li><li><p>Concise and direct. Cut filler and adjectives. Avoid the LLM-indicator words: comprehensive, leverage, robust, framework (when vague), utilise, facilitate, novel, landscape, foster, harness, streamline, pivotal, nuanced, multifaceted, cornerstone, synergy, overarching. Avoid filler and stance markers: very, significant, crucial, essential, clearly, obviously, &quot;it is important to note that&quot;. Delete the LLM connective openers <code>Furthermore</code>, <code>Moreover</code>, <code>Additionally</code>, <code>In conclusion</code>, <code>Overall</code>. Restrict genuine connectives to <code>However,</code> <code>Whilst</code>, <code>Yet</code>, <code>Unlike</code>, <code>Similarly</code>, <code>Alternatively</code>, <code>Finally,</code>, <code>Instead</code>.</p></li><li><p>Target 15-30 words per sentence and treat 40 as the ceiling. Exceed it only for a flat, colon-led enumeration, never for nested subordination. Use at most two commas per sentence for internal structure, a third only in a serial list.</p></li><li><p>Never use a dash as punctuation. Replace it with a full stop, or with parentheses when the aside is a gloss or example. Use a colon only to introduce a list or an expansion, never to join two independent statements. Use a semicolon only to separate list items that themselves contain commas.</p></li><li><p>Instead of a trailing qualifier (&quot;, which means that...&quot;, &quot;, thereby enabling...&quot;), cut the comma and start a new sentence with <code>This</code>, <code>These</code> or <code>It also</code>. Keep any surviving trailing <code>, which</code> clause to about one sentence in ten, and only where the clause carries the point rather than padding it.</p></li><li><p>Write paragraphs of 2-6 sentences. Open each with its claim, not with context, and close it with a consequence or a limitation, never a restatement of the opening.</p></li><li><p>Hedge with <code>can</code>, <code>could</code>, <code>may</code> or <code>potential</code>, one hedge per claim. Never stack hedges (&quot;may potentially&quot;, &quot;could possibly suggest&quot;).</p></li><li><p>Report intervals as sentences, without a leading median. Write the credible interval as a phrase, not a &quot;median (lower, upper)&quot; construction.</p></li><li><p>Report numbers with their provenance and units. Delete a vague quantity (&quot;a large number of&quot;, &quot;significantly more&quot;) rather than leave it unsourced.</p></li><li><p>UK English throughout.</p></li><li><p>Section and subsection titles are just the title. No descriptive suffix after a title (not &quot;Reproduction number — weekly random walk with intervention ramp&quot;, just &quot;Reproduction number&quot;), and no detail-dump in the first sentence after a heading.</p></li><li><p>Order the methods generatively, infections through to observation endpoints: the infection process first, then the epidemiological processes (delays, case-fatality ratio), then the observation models (surveillance streams before exports), then the joint model.</p></li><li><p>Define every quantity before it is used. Define the reproduction number before the seeding that relies on it; introduce the initial infection count before describing how it arises; define every symbol and operator (including convolution) the first time it appears. Never use a symbol the reader has not met. Give an equation once in display maths and refer to its symbols inline afterwards; never narrate a displayed equation in words.</p></li><li><p>Do not repeat. State a convention once (the credible-interval levels, the delay discretisation) and do not restate it per bullet or subsection. Cut sentences that duplicate earlier content.</p></li><li><p>Cite the source of each prior and carry the uncertainty the source reports. When a source gives a distribution with uncertainty (a shape and scale with intervals), propagate that, not a self-assigned weakly-informative spread. Do not write &quot;with an assumed weakly-informative spread&quot; repeatedly. If a prior is our own choice, say so plainly (&quot;we use a prior of ...&quot;).</p></li><li><p>State assumptions as assumptions (&quot;we assume a single seed case&quot;, &quot;we assume the response scale-up takes about three weeks&quot;). Do not assert a false rationale for a modelling choice (not &quot;a Poisson because the count is small&quot;).</p></li><li><p>Do not editorialise or justify priors in the narrative (not &quot;a diffuse prior would let the background absorb the whole stream&quot;). State what the model does.</p></li><li><p>Methods belong in the methods. Do not leave model description (the intervention model, the counterfactual, the forecast, the evaluation) in the results. Move it to the methods and keep the results to findings.</p></li><li><p>No project or issue history in the narrative: no PR or issue numbers, no &quot;we previously used X&quot;, &quot;this was changed to Y&quot;, &quot;after refactoring&quot;. State the current design only. A difference from work being replicated, or a limitation of an external dependency, is the exception, written as a present-tense fact. An issue number may also stay where the issue is the record for something still unresolved, such as a workaround pointing at the open root cause it does not fix, or a data decision whose reasoning lives in the issue. The test is whether the number still tells the reader something they cannot get from the code. &quot;Tracked by #495&quot; earns its place; &quot;changed in #495&quot; does not.</p></li><li><p>Label quantities accurately. Do not call suspected cases onsets, and prefer &quot;current cumulative&quot; over &quot;final cumulative&quot;.</p></li><li><p>For a latent quantity (infections, onsets, deaths) report the modelled estimate without overlaying observed data that sits downstream of unmodelled processes.</p></li><li><p>Plots use the same credible-interval ribbons as the tables, not a bare median, and show only the period being estimated rather than greying out the rest.</p></li><li><p>Model code shown in the report is clean. Strip working comments before it is displayed.</p></li><li><p>Flag a future improvement as a GitHub issue, not a buried caveat in the prose.</p></li><li><p>Bullet lists, bold and italics are advisory rather than enforced here, since this is a technical report rather than a journal manuscript. Do not add new bullet lists or bold to the narrative prose, and do not restructure an existing list that carries genuinely parallel content, but do strip bold or italics used for mid-sentence emphasis.</p></li></ul><h2 id="Pull-requests" tabindex="-1">Pull requests <a class="header-anchor" href="#Pull-requests" aria-label="Permalink to &quot;Pull requests {#Pull-requests}&quot;">​</a></h2><ul><li><p><code>main</code> is branch-protected. Changes go through pull requests.</p></li><li><p>Run the test suite before opening a pull request.</p></li><li><p>Add a bullet to the <a href="./news">News</a> page under <code>Unreleased</code> for any user-visible change.</p></li></ul>`,24)])])}const m=t(a,[["render",n]]);export{u as __pageData,m as default};
